"""Package Readiness Compiler package.

v1 skeleton: loader (pure parsing) -> validator (PKG rules) -> renderer
(in-memory previews + optional atomic write). Orchestrated by
scripts/compile_packages.py. Spec: wiki/ops/package-readiness-compiler-spec.md.
"""

__version__ = "0.1.0"
